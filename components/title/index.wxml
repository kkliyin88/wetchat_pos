<!--components/top/index.wxml-->
<view class='container'>
  <view class='title'  style='padding-top:{{statusBarHeight+30+"rpx"}}'>
    <view wx:if='{{backflag}}' class='goback' bindtap='goback'>
    <image class='fanhui' src="{{base_url+'fanhui.png'}}" class='left'></image>
    </view>
    <view>{{title}}</view>
  </view>
</view>